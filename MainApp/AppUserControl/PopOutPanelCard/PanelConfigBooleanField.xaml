<UserControl
    x:Class="MSFSPopoutPanelManager.MainApp.AppUserControl.PopOutPanelCard.PanelConfigBooleanField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:calcBinding="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:MSFSPopoutPanelManager.MainApp.ViewModel"
    d:DataContext="{d:DesignInstance viewModel:PanelConfigFieldViewModel}"
    mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <TextBlock
            Margin="0,3,0,0"
            Text="{Binding BindingPath}"
            ToolTip="Set this panel to be always on top" />
        <ToggleButton
            x:Name="TglBtn"
            Margin="0,2,0,0"
            IsChecked="{Binding BindingPath, Mode=TwoWay, NotifyOnSourceUpdated=True}"
            IsEnabled="{calcBinding:Binding !DataItem.FullScreen}"
            Style="{StaticResource ToggleButton}" />
    </StackPanel>
</UserControl>
